# 影游工坊 - 项目开发文档

## 项目概述

影游工坊是一款零代码拖拽式影视互动游戏编辑器，基于 Ren'Py 8.x 引擎。用户通过可视化时间轴拖拽分镜节点（背景、立绘、对话、音频、选项等），即可编排完整的互动叙事流程，编译器自动将项目数据转换为标准 Ren'Py `.rpy` 脚本。

项目采用纯浏览器端架构，所有素材和项目数据存储在浏览器 IndexedDB 中，无需安装桌面软件即可使用。同时保留 Electron 桌面端封装能力，可通过 Electron 打包为跨平台桌面应用。

### 核心特性

- 零代码可视化编辑：6 条轨道拖拽编排分镜，实时画布预览
- 浏览器原生存储：IndexedDB 持久化素材和项目数据，导入导出 ZIP 包
- Ren'Py 编译器：自动生成 `script.rpy`、`options.rpy`、`variables.rpy`、`screens.rpy`
- 拓扑排序与循环检测：基于场景跳转关系构建有向图，Kahn 算法排序并检测死循环
- 撤销/重做：基于 zundo 的 50 步历史记录
- 多平台部署：Vite 开发服务器、Docker 容器、Electron 桌面应用

### 技术栈

| 层级 | 技术 | 版本 | 用途 |
|------|------|------|------|
| 前端框架 | React | 18.2 | UI 组件 |
| 类型系统 | TypeScript | 5.3 | 类型安全 |
| 构建工具 | Vite | 5.0 | 开发服务器与打包 |
| 样式方案 | Tailwind CSS | 3.4 | 原子化 CSS |
| 状态管理 | Zustand | 4.5 | 全局状态 |
| 撤销/重做 | zundo | 2.1 | 时间旅行状态 |
| 拖拽排序 | @dnd-kit | 6.1 | 时间轴卡片排序 |
| 画布渲染 | PixiJS | 8.2 | 场景实时预览 |
| 浏览器存储 | IndexedDB | 原生 API | 素材 Blob 与项目数据 |
| 客户端打包 | JSZip | 3.10 | 导入导出 ZIP |
| 桌面端 | Electron | 28.x | 跨平台桌面应用 |
| 单元测试 | Vitest | 1.1 | 编译器与核心逻辑测试 |

---

## 目录结构

```
yingyou-workshop/
├── electron/                    # Electron 主进程
│   ├── main.ts                  # 主进程入口，窗口管理
│   ├── preload.ts               # 预加载脚本，暴露 IPC API
│   ├── tsconfig.json             # Electron TS 配置
│   └── ipc/                     # IPC 处理器
│       ├── project.ts           # 项目保存/加载/导出
│       ├── asset.ts             # 素材文件操作
│       └── renpy.ts             # Ren'Py 预览与编译
├── src/                         # 前端源码
│   ├── main.tsx                 # React 入口
│   ├── App.tsx                  # 根组件（启动器/编辑器切换）
│   ├── index.css                # 全局样式 + Tailwind
│   ├── types/                   # 类型定义
│   │   ├── index.ts             # 核心数据模型
│   │   └── factory.ts           # 节点工厂函数
│   ├── stores/                  # Zustand 状态管理
│   │   ├── projectStore.ts      # 项目数据 + 撤销/重做
│   │   ├── uiStore.ts           # UI 状态
│   │   └── toastStore.ts        # 全局通知
│   ├── services/                # 核心业务服务
│   │   ├── compiler.ts          # Ren'Py 编译器
│   │   ├── linter.ts            # 代码检查器
│   │   ├── topologicalSort.ts   # 场景拓扑排序
│   │   ├── fileService.ts       # 文件操作服务
│   │   └── idb.ts               # IndexedDB 封装
│   └── components/              # React 组件
│       ├── Launcher/            # 启动器
│       ├── Editor/              # 编辑器布局
│       ├── LeftPanel/           # 左侧面板
│       ├── Timeline/            # 时间轴
│       ├── Canvas/              # 画布预览
│       ├── Inspector/           # 属性检查器
│       ├── Modals/              # 弹窗
│       └── ui/                  # 通用 UI 组件
├── tests/                       # 单元测试
│   ├── compiler.test.ts
│   └── store-verification.test.ts
├── docs/                        # 文档与截图
├── package.json
├── tsconfig.json
├── vite.config.ts
├── tailwind.config.js
├── Dockerfile
├── docker-compose.yml
└── .gitignore
```

---

## 架构设计

### 整体架构

应用分为两个视图层级：启动器（Launcher）和编辑器（EditorLayout）。`App.tsx` 通过 `view` 状态在两者间切换。

```
App.tsx
├── Launcher（启动器）
│   ├── 最近项目列表（从 IndexedDB 读取）
│   ├── 新建项目（弹窗填写元数据）
│   └── 导入项目（读取 .yypkg ZIP）
└── EditorLayout（编辑器）
    ├── Toolbar（顶部工具栏）
    ├── LeftPanel（左侧面板：素材/角色/变量）
    ├── SceneNavigator（场景导航栏）
    ├── CanvasPreview（画布预览）
    ├── Timeline（时间轴）
    ├── Inspector（属性检查器）
    └── ProblemsPanel（问题面板）
```

### 数据流

所有项目数据存储在 `projectStore` 的 `data` 字段中，结构为 `ProjectData`。UI 组件通过 Zustand 的 selector 订阅数据变化，操作时调用 store 方法修改数据，zundo 自动记录历史。编译器和 Linter 读取 `data` 生成 Ren'Py 脚本和检查结果。

```
用户操作 → projectStore.setData() → zundo 记录历史
                ↓
          组件自动重渲染
                ↓
    CanvasPreview 重绘 / Inspector 更新表单
                ↓
    用户点击导出 → compiler.compileProject(data)
                ↓
    生成 .rpy 脚本 → 打包为 .yypkg 下载
```

### 浏览器存储架构

由于浏览器端无法直接访问本地文件系统，项目采用 IndexedDB 作为持久化方案：

| 存储区域 | 用途 | 数据格式 |
|----------|------|----------|
| `assets` | 素材文件 Blob | `assetId → Blob` |
| `projects` | 项目数据 | `projectKey → JSON 字符串` |
| `meta` | 元数据（最近项目列表等） | `key → value` |

素材导入时，`File` 对象以 Blob 形式存入 IndexedDB，运行时通过 `URL.createObjectURL()` 生成临时 URL 供 `<img>`、`<video>` 和 PixiJS 使用。URL 使用缓存机制避免重复创建导致内存泄漏。

项目导出时，JSZip 将 `project.json` + Ren'Py 脚本 + 素材文件打包为 `.yypkg`（ZIP 格式）下载。导入时反向操作，从 ZIP 恢复项目数据和素材到 IndexedDB。

---

## 核心数据模型

所有类型定义在 `src/types/index.ts` 中。

### 项目元数据 ProjectMeta

```typescript
interface ProjectMeta {
  version: '1.0';
  name: string;
  author: string;
  resolution: { width: number; height: number };
  defaultFont: string;
  themeColor: string;          // hex 格式
  createdTime: string;         // ISO 8601
  lastModified: string;
  renpyVersion: string;        // '8.2.3'
  sceneOrder: string[];        // 场景 ID 列表，决定顺序
  currentSceneId: string | null;
}
```

`sceneOrder` 是场景的有序列表，决定编译时的场景排列顺序。`currentSceneId` 记录当前正在编辑的场景。

### 素材 AssetEntry

```typescript
type AssetType = 'background' | 'sprite' | 'video' | 'bgm' | 'sfx' | 'voice';
type AssetFormat = 'png' | 'jpg' | 'jpeg' | 'mp4' | 'webm' | 'ogg' | 'mp3' | 'wav';

interface AssetEntry {
  id: string;                  // UUID
  fileName: string;
  relativePath: string;        // 相对于 assets/ 的路径
  type: AssetType;
  format: AssetFormat;
  fileSize: number;
  resolution?: { width: number; height: number };
  duration?: number;           // 视频/音频时长（秒）
  characterId?: string;        // 立绘关联的角色 ID
  emotion?: string;            // 立绘表情名
  tags: string[];
  thumbnailPath: string;
  importedAt: string;
}
```

素材类型在导入时确定。用户在素材库选择导入类型（背景/立绘/视频/BGM/音效/语音），系统直接使用用户选择的类型，不再依赖文件名猜测。对于立绘素材，系统会尝试从文件名解析角色 ID 和表情（如 `alice_happy` → `characterId: alice, emotion: happy`）。

### 角色 CharacterDef

```typescript
interface CharacterDef {
  id: string;                  // 字母开头，仅含字母数字下划线
  displayName: string;
  color: string;               // hex
  voicePrefix: string;
  defaultEmotion: string;
  emotions: Record<string, string>;  // emotion -> assetId
}
```

角色 ID 直接作为 Ren'Py 的 `Character` define 名，因此有严格的命名约束（编译时通过 `sanitizeLabel` 清理非法字符）。

### 变量 VariableDef

```typescript
type VariableType = 'integer' | 'float' | 'string' | 'boolean';

interface VariableDef {
  name: string;                // 字母开头，仅含字母数字下划线
  type: VariableType;
  initialValue: number | string | boolean;
  description?: string;
}
```

### 场景与节点

```typescript
interface Scene {
  id: string;                  // UUID
  name: string;
  nodes: SceneNode[];
  color?: string;              // 导航栏卡片颜色
}
```

场景包含若干分镜节点，节点按 `trackIndex`（轨道）和 `position`（轨道内顺序）组织。

### 节点类型

共有 8 种节点类型，继承自 `BaseNode`：

```typescript
interface BaseNode {
  id: string;                  // UUID
  type: NodeType;
  trackIndex: number;          // 0=背景, 1=立绘, 2=对话, 3=音频, 4=选项, 5=跳转
  position: number;            // 轨道内顺序索引
  label?: string;              // 可选标签
}
```

| 节点类型 | 轨道 | 说明 | 关键字段 |
|----------|------|------|----------|
| `background` | 0 | 背景图片切换 | `assetId`, `transition`, `transitionDuration` |
| `sprite` | 1 | 角色立绘显示/隐藏 | `characterId`, `emotion`, `screenPosition`, `enterEffect` |
| `dialogue` | 2 | 对话文本 | `speakerId`, `text`, `textStyle`, `autoAdvance` |
| `audio` | 3 | 音频播放控制 | `assetId`, `audioType`, `action`, `volume`, `loop` |
| `choice` | 4 | 玩家选项菜单 | `choices[]`, `layout`, `buttonStyle` |
| `video` | 0 | 视频播放 | `assetId`, `playMode`, `skipAllowed` |
| `jump_label` | 5 | 跳转/标签/返回 | `subType`, `targetLabel`, `labelName` |
| `variable_op` | 5 | 变量操作 | `variableName`, `operation`, `value` |

`SpriteNode` 中 `screenPosition` 表示屏幕位置（left/center/right/truecenter），与 `BaseNode.position`（轨道内顺序索引）语义完全不同，使用不同字段名避免混淆。

### 选项节点 ChoiceItem

```typescript
interface ChoiceItem {
  id: string;
  text: string;
  targetSceneId: string;       // 跳转目标场景 ID
  condition?: string;           // 如 "trust >= 80"
  variableEffects?: VariableEffect[];
}
```

`targetSceneId` 存储的是场景 UUID，编译器通过 `sceneIdToName` 映射表将其解析为场景名，再 sanitize 为合法 label。

### 轨道定义

```typescript
const TRACKS: TrackDef[] = [
  { index: 0, name: '背景', maxConcurrent: 1, acceptedTypes: ['background'] },
  { index: 1, name: '立绘', maxConcurrent: 3, acceptedTypes: ['sprite'] },
  { index: 2, name: '对话', maxConcurrent: 1, acceptedTypes: ['dialogue'] },
  { index: 3, name: '音频', maxConcurrent: 3, acceptedTypes: ['audio'] },
  { index: 4, name: '选项', maxConcurrent: 1, acceptedTypes: ['choice'] },
  { index: 5, name: '跳转/标签', maxConcurrent: Infinity, acceptedTypes: ['jump_label', 'variable_op'] },
];
```

### 完整项目数据

```typescript
interface ProjectData {
  meta: ProjectMeta;
  scenes: Record<string, Scene>;  // sceneId -> Scene
  assets: AssetEntry[];
  characters: CharacterDef[];
  variables: VariableDef[];
}
```

---

## 状态管理

### projectStore

核心状态管理器，位于 `src/stores/projectStore.ts`。使用 Zustand + zundo 实现数据管理和撤销/重做。

#### 状态结构

```typescript
interface ProjectStoreState {
  data: ProjectData;           // 完整项目数据
  isDirty: boolean;             // 是否有未保存修改
  // 项目级操作
  loadProject: (data: ProjectData) => void;
  newProject: (name: string) => void;
  updateMeta: (partial: Partial<ProjectMeta>) => void;
  markClean: () => void;
  // 场景操作
  addScene: (name?: string) => string;
  deleteScene: (sceneId: string) => void;
  renameScene: (sceneId: string, name: string) => void;
  reorderScenes: (newOrder: string[]) => void;
  setCurrentScene: (sceneId: string) => void;
  // 节点操作
  addNode: (sceneId, nodeType, position?) => string;
  addNodeFromAsset: (sceneId, assetId, trackIndex?, position?) => string;
  deleteNode: (sceneId, nodeId) => void;
  updateNode: (sceneId, nodeId, partial) => void;
  moveNode: (sceneId, nodeId, newTrack, newPosition) => void;
  reorderNodes: (sceneId, trackIndex, newOrder) => void;
  // 素材/角色/变量操作
  addAsset / updateAsset / deleteAsset / getAsset
  addCharacter / updateCharacter / deleteCharacter / getCharacter
  addVariable / updateVariable / deleteVariable
  // 编译器接口
  getProjectData: () => ProjectData;
}
```

#### 撤销/重做配置

zundo 配置了以下参数：

- `limit: 50` — 最多保留 50 步历史
- `partialize` — 只追踪 `data` 字段，排除 `isDirty`
- `equality` — 使用 `JSON.stringify` 深比较判断状态是否变化

`loadProject` 和 `newProject` 在设置数据后会调用 `temporal.clear()` 清除历史，防止跨项目撤销。由于 `set()` 本身会产生一条历史记录，因此清除操作执行两次以确保完全干净。

#### 节点 position 管理

添加节点时，新节点插入到指定 position，所有 position >= 插入点的同轨道节点自动后移。删除节点时，所有 position > 删除点的节点自动前移。移动节点时，同时修复源轨道和目标轨道的 position 序列。

### uiStore

管理编辑器界面状态，不涉及项目数据。包括选中状态（节点/素材/角色）、面板开关、模态框开关、预览/导出进度等。

### toastStore

全局通知系统，提供 `success`、`error`、`warning`、`info` 四种级别，自动消失。

---

## 编译器实现

编译器位于 `src/services/compiler.ts`，负责将 `ProjectData` 转换为标准 Ren'Py 脚本。

### 编译流程

```
compileProject(project)
  ├── 1. 初始化：构建 assetMap、variableNames、sceneLabels、sceneIdToName
  ├── 2. 拓扑排序：topologicalSort(project) 排序场景并检测循环
  ├── 3. 生成图片声明：image bg_xxx = "images/xxx.png"
  ├── 4. 生成角色声明：define xxx = Character("显示名", color="#xxx")
  ├── 5. 生成变量初始化：default xxx = 0
  ├── 6. 生成游戏入口：label start: / jump first_scene
  ├── 7. 遍历场景，编译每个场景为 label
  └── 8. 生成 options.rpy / variables.rpy / screens.rpy
```

### 游戏入口处理

编译器检查第一个场景的名称是否为 `start`：
- 如果第一个场景名经 sanitize 后为 `start`，直接使用该场景作为入口，不再重复生成 `label start:`
- 如果第一个场景名不是 `start`，生成 `label start:` 并在其中 `jump` 到第一个场景
- 如果没有任何场景，生成空的 `label start:`

这避免了 `label start:` 重复声明导致的编译错误。

### 节点编译映射

| 节点类型 | 生成的 Ren'Py 代码 |
|----------|-------------------|
| background | `scene bg_xxx` + `with Dissolve(1.0)` |
| sprite | `show charId emotion at center zorder 1` + `with easeinleft` |
| dialogue | `charId "文本"` 或 `"文本"`（旁白）|
| audio | `play music "file.ogg" fadein 2` / `stop music fadeout 1` |
| video | `$ renpy.movie_cutscene("images/file.mp4")` |
| choice | `menu:` + `"选项文本" if condition:` + `$ var += 1` + `jump target` |
| jump_label | `label xxx:` / `jump xxx` / `return` |
| variable_op | `$ varName = value` |

### 跳转目标解析

`resolveJumpTarget` 方法将选项的 `targetSceneId`（UUID 格式）解析为可跳转的 label 名。通过 `sceneIdToName` 映射表查找场景名，再通过 `sanitizeLabel` 转换为合法的 Ren'Py label 名。`sanitizeLabel` 保留中文字符（`[\u4e00-\u9fa5]`），支持中文场景名。

### 条件选项

条件选项使用 Ren'Py 的行内 if 语法：

```renpy
menu:
    "选项文本" if trust >= 80:
        $ trust += 10
        jump target_scene
    "另一个选项":
        jump another_scene
```

### 辅助文件生成

| 文件 | 内容 |
|------|------|
| `options.rpy` | `config.name`、`config.screen_width/height`、`gui.text_font` 等全局配置 |
| `variables.rpy` | `init python:` 块中初始化所有变量 |
| `screens.rpy` | 基础 `say` 界面定义（MVP 使用默认界面） |

---

## 拓扑排序与循环检测

位于 `src/services/topologicalSort.ts`，基于场景间的跳转关系构建有向图。

### 边收集

遍历所有场景的节点，收集跳转边：
- `choice` 节点的每个选项如果指定了 `targetSceneId`，则添加 `当前场景 → 目标场景` 的边
- `jump_label` 节点的 `targetLabel` 如果匹配某个场景名或场景 ID，同样添加边

### Kahn 算法

1. 计算所有场景的入度
2. 将入度为 0 的场景按 `sceneOrder` 中的优先级排序后入队
3. 依次出队，将其指向的场景入度减 1，新入度为 0 的场景入队
4. 如果排序后的场景数 < 总场景数，说明存在环

存在环时，未访问的场景仍会追加到排序结果中（不丢失数据），但会返回 `hasCycle: true` 和 `cycleScenes` 列表供编译器和 Linter 报警。

---

## Linter 代码检查

位于 `src/services/linter.ts`，在编译前和预览前校验项目数据。

### 检查项

| 检查项 | 级别 | 说明 |
|--------|------|------|
| 循环跳转 | warning | 拓扑排序检测到的环 |
| 场景数超限 | warning | 超过 200 个场景 |
| 变量数超限 | warning | 超过 100 个变量 |
| 节点数超限 | warning | 单场景超过 500 个节点 |
| 素材引用不存在 | error | 节点引用的 assetId 在素材库中找不到 |
| 角色引用不存在 | error | 立绘节点的 characterId 未定义 |
| 对话内容为空 | warning | dialogue 节点 text 为空 |
| 跳转目标不存在 | warning | jump 的目标场景/label 不存在 |
| 变量未定义 | error | 变量操作/条件中引用了未定义的变量 |
| 除以零 | error | 变量操作中 divide 且值为 0 |
| 变量名不合法 | error | 不符合 `^[a-zA-Z_][a-zA-Z0-9_]*$` |
| 角色ID不合法 | error | 不符合 `^[a-zA-Z_][a-zA-Z0-9_]*$` |
| 条件表达式语法错误 | error | 不含合法的比较运算符 |

### 条件表达式校验

支持形如 `trust >= 80`、`has_key == True`、`name == "abc"` 的表达式。如果不含比较运算符，则检查是否为纯布尔变量名或 `True`/`False`。同时从条件中提取变量名并校验是否已定义。

---

## 浏览器存储方案

### IndexedDB 封装

`src/services/idb.ts` 封装了 IndexedDB 操作，包含三个 Object Store：

```
yingyou-workshop (DB)
├── assets    (assetId → Blob)
├── projects  (projectKey → JSON string)
└── meta      (key → value)
```

提供以下 API：

| 函数 | 用途 |
|------|------|
| `saveAssetBlob(assetId, blob)` | 存储素材文件 |
| `getAssetBlob(assetId)` | 获取素材 Blob |
| `deleteAssetBlob(assetId)` | 删除素材 |
| `getAllAssetBlobs()` | 获取所有素材（导出用） |
| `clearAllAssets()` | 清空素材（导入前清理） |
| `saveProjectToIDB(key, data)` | 保存项目数据 |
| `loadProjectFromIDB(key)` | 加载项目数据 |
| `getMeta(key)` / `setMeta(key, value)` | 元数据读写 |
| `getAssetURL(assetId)` | 获取素材 Object URL（带缓存） |

Object URL 使用 `Map<string, string>` 缓存，避免重复创建导致内存泄漏。`revokeAssetURL` 和 `clearURLCache` 用于显式释放。

### 文件服务

`src/services/fileService.ts` 是文件操作的统一入口，处理浏览器和 Electron 两种环境。

#### 素材导入流程

1. 用户在素材库点击「导入素材」并选择类型（背景/立绘/视频/BGM/音效/语音）
2. 调用 `selectFiles(accept)` 打开系统文件选择器
3. `importAssetFiles(files, forceType)` 将文件存入 IndexedDB
   - 如果传入了 `forceType`，直接使用用户选择的类型
   - 否则根据文件扩展名推断类型
   - 对于立绘素材，尝试从文件名解析 `characterId` 和 `emotion`
   - 图片文件额外获取尺寸信息
4. 返回 `AssetEntry[]` 元数据，存入 projectStore

#### 项目保存

`saveProject(data)` 将项目数据序列化为 JSON 存入 IndexedDB 的 `autosave` 键，并更新最近项目列表。

#### 项目导出

`exportProjectPackage(data, scripts...)` 使用 JSZip 打包：
- `project.json` — 完整项目数据
- `renpy/script.rpy` / `options.rpy` / `variables.rpy` / `screens.rpy` — 编译生成的脚本
- `assets/` — 所有素材文件

打包后生成 `.yypkg` 文件（ZIP 格式）触发浏览器下载。

#### 项目导入

`importProjectPackage(file)` 从 `.yypkg` ZIP 文件恢复：
1. 读取 `project.json` 解析项目数据
2. 清空旧 IndexedDB 素材
3. 从 `assets/` 目录恢复素材文件到 IndexedDB
4. 保存项目数据到 IndexedDB 并更新最近列表

---

## UI 组件

### 启动器 Launcher

启动器是应用的入口界面，左侧显示最近项目列表（从 IndexedDB 读取），右侧提供新建和导入操作。

- **新建项目**：弹出模态框填写项目名、作者、分辨率、字体、主题色，直接创建空项目进入编辑器
- **导入项目**：选择 `.yypkg` ZIP 文件，恢复项目数据和素材
- **最近项目**：点击打开，悬停显示删除按钮

### 编辑器布局 EditorLayout

主编辑器采用三栏布局：

```
┌──────────────────────────────────────────┐
│              Toolbar（48px）              │
├──────┬──────────────────────┬────────────┤
│      │   SceneNavigator      │            │
│ Left │  ┌──────────────────┐ │  Inspector │
│Panel │  │                  │ │  (280px)   │
│(260px│  │  CanvasPreview   │ │            │
│      │  │   (flex-1)       │ │            │
│      │  │                  │ │            │
│      │  └──────────────────┘ │            │
│      │   Timeline (220px)    │            │
└──────┴──────────────────────┴────────────┘
```

### 工具栏 Toolbar

顶部工具栏包含：返回启动器、预览、保存、导出、添加节点（下拉菜单）、撤销/重做、问题面板、项目设置、折叠左侧面板。

添加节点下拉菜单使用点击触发方式，监听 `mousedown` 和 `Escape` 关闭，避免与拖拽事件冲突。

### 左侧面板 LeftPanel

通过标签页切换三个面板：

1. **素材库 AssetLibrary**：顶部导入按钮（下拉选择类型）+ 搜索框 + 筛选标签，下方网格显示素材缩略图。缩略图从 IndexedDB 加载真实图片，支持右键菜单（添加到场景/重命名/复制路径/删除）。素材卡片可拖拽到时间轴轨道。
2. **角色管理 CharacterManager**：角色列表 + 新增/编辑/删除
3. **变量管理 VariableManager**：变量列表 + 新增/编辑/删除

### 场景导航栏 SceneNavigator

水平排列所有场景卡片，卡片间用 `→` 字符连接。支持：
- 点击切换当前场景
- 拖拽排序场景
- 右键菜单（重命名/删除）
- 添加新场景

### 时间轴 Timeline

6 条轨道水平排列分镜卡片。每条轨道显示轨道名称和节点计数（`当前/最大`）。

- **轨道内排序**：使用 @dnd-kit/sortable 实现拖拽排序
- **素材拖入**：支持从素材库拖拽素材到轨道，通过 HTML5 Drag and Drop API 传输 `assetId` 和 `assetType`，自动创建对应类型的节点
- **轨道验证**：拖入时检查素材类型是否匹配轨道的 `acceptedTypes`

TimelineCard 组件将拖拽手柄（图标区域）与点击区域分离，避免拖拽操作拦截节点选中事件。

### 画布预览 CanvasPreview

使用 PixiJS 8.x 渲染当前场景的实时预览。

#### 初始化流程

PixiJS 初始化时延迟一帧（`setTimeout 0`），避免 React StrictMode 双重挂载导致的竞态条件。初始化完成后创建 `Container` 并设置 `sortableChildren = true` 以支持 zorder。

#### 纹理加载

预加载当前场景引用的素材纹理：
- 背景节点：直接使用 `assetId` 查找纹理
- 立绘节点：通过 `characterId + emotion` 匹配素材

纹理使用 `Map<string, Texture>` 缓存，只加载未缓存的纹理。加载完成后触发重绘。

#### 渲染逻辑

节点按 `position`（升序）和 `trackIndex`（升序）排序后依次渲染：

| 节点类型 | 渲染方式 |
|----------|----------|
| background | 优先使用真实图片（等比缩放铺满），否则主题色矩形 |
| sprite | 优先使用真实立绘图片（高度 78%），否则占位矩形 + 角色名标签 |
| dialogue | 底部半透明黑框 + 说话人名（角色色）+ 正文 |
| choice | 画布中央垂直排列选项按钮 |

画布通过 ResizeObserver 监听容器尺寸变化，等比缩放适配。

### 属性检查器 Inspector

根据选中节点的类型显示对应的属性编辑表单。每种节点类型有独立的表单组件：

- **BackgroundForm**：背景素材选择、过渡类型/时长、等待过渡开关
- **SpriteForm**：角色选择、表情、屏幕位置、进入/退出效果、zorder、可见性
- **DialogueForm**：说话人选择、文本输入、字号/颜色/对齐、打字机速度、自动推进
- **AudioForm**：音频素材选择、类型、动作（播放/停止/暂停/恢复）、音量、淡入/淡出、循环
- **VideoForm**：视频素材、播放模式、音量、允许跳过
- **ChoiceForm**：布局选择、选项列表（文本/目标场景/条件/变量效果）、添加/删除选项
- **JumpLabelForm**：子类型选择（label/jump/return）、标签名/目标标签
- **VariableOpForm**：变量选择、操作符、值（根据变量类型显示不同输入控件）

### 弹窗

- **ExportModal**：编译项目并导出，展示编译问题列表和导出进度条
- **VariableModal**：变量创建/编辑
- **CharacterModal**：角色创建/编辑

---

## 节点工厂

`src/types/factory.ts` 提供所有节点类型的创建函数，统一生成带 UUID 的默认实例。

### 工厂函数

```typescript
createBackgroundNode(position, assetId?)    // 默认 dissolve 过渡，1.0 秒
createSpriteNode(position, characterId?, emotion?)  // 默认 center 位置
createDialogueNode(position, speakerId?)   // 默认 22px 白字
createVideoNode(position, assetId?)         // 默认允许跳过
createAudioNode(position, assetId?, audioType?)  // BGM 默认循环
createChoiceNode(position)                  // 默认 1 个选项
createJumpLabelNode(position, subType?)     // label/jump/return
createVariableOpNode(position)              // 默认 set 操作
```

### 素材到节点映射

`ASSET_TO_NODE_TYPE` 定义素材类型到节点类型的映射：

| 素材类型 | 节点类型 |
|----------|----------|
| background | background |
| sprite | sprite |
| video | video |
| bgm | audio |
| sfx | audio |
| voice | audio |

`createNodeFromAsset(asset, position)` 根据素材类型创建对应节点，自动填充 `assetId`、`characterId`、`emotion`、`audioType` 等字段。

`canAssetDropOnTrack(assetType, acceptedTypes)` 判断素材类型是否可拖入指定轨道。

---

## Electron 桌面端

### 主进程 main.ts

- 创建 BrowserWindow（1400x900，最小 1024x700）
- 开发模式加载 `http://localhost:5173`，生产模式加载 `dist/index.html`
- 注册 IPC 处理器：项目操作、素材操作、Ren'Py 预览
- 提供文件夹选择和文件选择对话框

### 预加载脚本 preload.ts

通过 `contextBridge` 暴露安全的 IPC API：

```typescript
window.electronAPI = {
  saveProject, loadProject,
  selectFolder, selectFiles,
  importAssets,
  exportProject,
  startPreview,
  onAssetWatcher,
}
```

### IPC 处理器

| 模块 | 功能 |
|------|------|
| `ipc/project.ts` | 项目保存/加载、导出 Ren'Py 工程 |
| `ipc/asset.ts` | 素材文件导入、素材监听（chokidar） |
| `ipc/renpy.ts` | Ren'Py 预览启动、SDK 调用 |

Electron 环境下，文件选择器返回文件路径，通过 IPC 读取实际文件内容。浏览器环境下，使用 `<input type="file">` 获取 `File` 对象。

---

## 开发与部署

### 环境要求

- Node.js >= 18
- npm >= 9

### 开发命令

```bash
# 安装依赖
npm install

# 启动开发服务器（浏览器模式）
npm run dev
# 访问 http://localhost:5173

# 启动 Electron 开发模式
npm run dev:electron

# 类型检查
npm run lint

# 构建
npm run build

# 构建 Electron
npm run build:electron

# 运行测试
npm test
```

### Vite 配置

```typescript
{
  plugins: [react()],
  resolve: { alias: { '@': './src' } },
  base: './',              // 相对路径，支持 Electron file:// 加载
  server: { port: 5173, strictPort: true },
  build: { outDir: 'dist', emptyOutDir: true },
  test: { globals: true, environment: 'node', include: ['tests/**/*.test.ts'] },
}
```

### TypeScript 配置

- `target: ES2020`
- `strict: true`
- `moduleResolution: bundler`
- 路径别名 `@/*` → `src/*`

### Docker 部署

使用多阶段构建的 Dockerfile：

1. **构建阶段**：`node:20-alpine`，安装依赖并执行 `npm run build`
2. **运行阶段**：`node:20-alpine`，安装 `serve` 静态文件服务器，复制 `dist` 目录

```bash
# 构建并运行
docker compose up -d
# 访问 http://localhost:5173

# 或手动构建
docker build -t yingyou-workshop .
docker run -p 5173:5173 yingyou-workshop
```

`docker-compose.yml` 配置了健康检查（`wget --spider` 检测 HTTP 服务可用性）和自动重启策略。

---

## 单元测试

测试文件位于 `tests/` 目录，使用 Vitest 运行。

### compiler.test.ts

测试编译器的核心功能：
- 基本编译流程（场景 → label）
- 各节点类型的代码生成
- 跳转目标解析（UUID → 场景名）
- 条件选项语法
- 变量操作
- 图片/角色/变量声明生成

### store-verification.test.ts

测试 projectStore 的状态管理：
- 添加/删除/更新节点
- position 管理
- 撤销/重做
- 项目加载后历史清除

---

## 关键设计决策

### 浏览器优先架构

项目最初设计为 Electron 桌面应用，依赖本地文件系统。在开发过程中发现浏览器端无法自由访问本地目录，因此重构为浏览器优先架构：

- 所有素材以 Blob 存入 IndexedDB，通过 Object URL 使用
- 项目数据自动保存到 IndexedDB，无需选择路径
- 导入/导出通过 ZIP 文件（`.yypkg`）实现
- Electron 作为可选封装层，浏览器模式是主要使用方式

### position 与 screenPosition 区分

`BaseNode.position` 表示节点在轨道内的顺序索引（整数），`SpriteNode.screenPosition` 表示立绘在屏幕上的位置（left/center/right/truecenter）。两者语义完全不同，使用不同字段名避免混淆。

### 拖拽手柄与点击区域分离

TimelineCard 中，dnd-kit 的 `listeners` 只绑定在图标区域（拖拽手柄），卡片其余区域处理 `onClick` 选中节点。这避免了拖拽操作拦截点击事件导致节点无法选中的问题。

### 下拉菜单的关闭策略

工具栏的「添加节点」下拉菜单使用点击触发方式（而非 hover），同时监听 `mousedown`（外部点击）和 `keydown`（Escape）关闭菜单。素材库的导入类型下拉菜单也采用相同策略。

### label start 重复声明处理

如果第一个场景名为 `start`，编译器直接使用该场景作为入口，不再生成 `label start:`，避免与场景自身的 label 声明冲突。

### 立绘素材类型识别

素材导入时接受 `forceType` 参数。用户在素材库选择「立绘」导入时，直接将素材类型设为 `sprite`，不再依赖文件名正则猜测。文件名解析（`characterId_emotion`）仅用于自动填充立绘元数据，不影响类型判断。

---

## Ren'Py 兼容性

### 生成的文件结构

导出的 `.yypkg` 包含以下结构：

```
项目名.yypkg
├── project.json              # 完整项目数据（JSON）
├── renpy/
│   ├── script.rpy            # 主脚本（image/define/default/label）
│   ├── options.rpy           # 全局配置
│   ├── variables.rpy         # 变量初始化
│   └── screens.rpy           # 界面定义
└── assets/                   # 素材文件
    ├── bg_xxx.png
    ├── alice_happy.png
    └── bgm_xxx.ogg
```

### script.rpy 生成规则

1. **文件头注释**：项目名、作者、生成时间
2. **图片声明**：`image bg_xxx = "images/xxx.png"`（背景）、`image charId emotion = "images/xxx.png"`（立绘）
3. **角色声明**：`define charId = Character("显示名", color="#xxx")`
4. **变量初始化**：`default varName = initialValue`
5. **游戏入口**：`label start:` + `jump first_scene`
6. **场景脚本**：每个场景编译为 `label scene_name:` + 节点代码

### 已知的兼容性限制

- **素材路径**：编译器生成的素材路径为 `images/xxx`，实际导出时素材放在 `assets/` 目录，需要在 Ren'Py 工程中调整目录结构或添加路径配置
- **视频路径**：视频节点生成 `renpy.movie_cutscene("images/xxx.mp4")`，同样存在路径不一致
- **音频路径**：音频节点直接使用文件名（如 `play music "xxx.ogg"`），需要确保文件在 Ren'Py 的音频搜索路径中
- **过渡效果**：`transitionDuration` 参数被直接传入 `Dissolve(1.0)` 等，单位为秒，符合 Ren'Py 规范
- **条件表达式**：生成的 `if condition:` 语法需要确保条件表达式中的变量已在 Ren'Py 中定义
- **screens.rpy**：MVP 版本只生成基础的 `say` 界面，未包含完整的选项按钮样式自定义

### 使用导出的工程

1. 解压 `.yypkg` 文件
2. 将 `renpy/` 目录下的 `.rpy` 文件复制到 Ren'Py 工程的 `game/` 目录
3. 将 `assets/` 目录下的素材文件复制到 `game/images/` 和 `game/audio/` 目录
4. 在 Ren'Py SDK 中打开工程并运行

---

## 已知问题与改进方向

### 当前限制

- **预览功能**：浏览器端预览降级为新窗口展示生成的 `.rpy` 脚本文本，未实现真正的 Ren'Py 运行时预览
- **素材路径映射**：导出的素材路径与 Ren'Py 工程的标准目录结构不完全一致，需要手动调整
- **选项按钮样式**：`buttonStyle` 配置项在编译时未生成对应的 Ren'Py screen 样式代码
- **视频播放**：视频节点使用 `renpy.movie_cutscene`，对于 Web 平台可能不兼容
- **多语言支持**：界面仅支持中文

### 改进方向

- 集成 Ren'Py Web 版本实现浏览器内真实预览
- 自动处理素材路径映射，生成标准的 Ren'Py 工程目录结构
- 实现选项按钮的 screen 样式生成
- 添加素材预览播放（音频/视频）
- 支持场景节点的复制/粘贴
- 添加快捷键系统（Ctrl+S 保存、Ctrl+Z 撤销等）
- 支持项目版本管理和差异对比
- 添加模板库（预设场景模板、角色模板）
